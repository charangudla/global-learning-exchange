export type AccountVerificationStatus = "pending" | "verified" | "blocked";

export type SpeakerVerificationStatus =
  | "verification_pending"
  | "verified"
  | "limited_verified"
  | "needs_review"
  | "suspended"
  | "blocked";
